# Supabase client for database and storage
supabase==1.0.3

# MediaPipe for pose estimation (CPU-only)
mediapipe==0.10.9

# OpenCV for video processing (headless for servers)
opencv-python-headless==4.9.0.80

# NumPy for numerical operations
numpy==1.26.4

# FFmpeg wrapper for HDR to SDR transcoding
ffmpeg-python==0.2.0

# HTTP client for Edge Function API calls
requests==2.31.0

# AWS SDK for Secrets Manager
boto3==1.34.0
