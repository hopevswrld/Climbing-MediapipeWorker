# Railway configuration for background worker
# This service does NOT serve HTTP - it's a pure background worker

[deploy]
# Disable health checks entirely - this is not a web service
numReplicas = 1
restartPolicyType = "ON_FAILURE"

# No health check path - worker services don't need them
# healthcheckPath is intentionally omitted
